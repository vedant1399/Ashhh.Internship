# Task 5 - Frontend (React Vite + Axios)

Included: `react-task-manager.zip` which contains the Vite React frontend (Login + Dashboard skeleton).
Unzip and run:
```
cd react-task-manager
npm install
npm run dev
```
